<!-- One or two paragraphs: what this changes, and why it is the right change.
     CONTRIBUTING.md has the style, and CLAUDE.md has the rules that any change
     touching profiles, chat stores or the keychain has to hold to. -->

## How it was tested

<!-- Checks added to Tests/main.swift, or what you did by hand and to which
     profiles if the suite cannot reach it. `./test.sh` passing is the floor,
     not the answer. -->

## What it means for profiles that already exist

<!-- Delete this heading if the change cannot touch anybody's chats. Otherwise:
     what happens on the next launch to a profile grafted under the old
     behaviour, and whether anything it does can be undone. -->
